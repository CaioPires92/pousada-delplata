# n8n Resetado

O diretório `n8n/` foi zerado intencionalmente.

Estado atual:

- nenhum workflow ativo está versionado aqui;
- a emissão do CRM para `N8N_WEBHOOK_URL` está desativada no código;
- qualquer nova automação deve ser redesenhada do zero antes de voltar ao repositório.

Quando retomarmos:

1. definir o fluxo mínimo desejado;
2. criar a stack operacional limpa do n8n;
3. versionar apenas os workflows realmente validados.
